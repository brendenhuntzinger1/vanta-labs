# Playwright MCP scratch output — page snapshots, console logs, traces written
# by the browser server during a session. Development artefacts, never source.
.playwright-mcp/
test-results/

node_modules/

# Stray Playwright screenshots written to the repo root when a custom
# filename is passed to browser_take_screenshot. Also scratch, never source.
*-mobile.png
*-desktop.png
page-*.png
